To list hashes of commits in one line with branches they were commited 

```
git log --oneline --graph --branches
```

we can reset like this

```
git reset --hard <tag>
```

you can merge like you did before, just pay attention whn solving conflicts 